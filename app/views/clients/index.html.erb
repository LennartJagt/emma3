<p id="notice"><%= notice %></p>

<h1>Clients</h1>

<table>
  <thead>
	<tr>Totaal aantal cliënten:<%= @clients.count %></tr>
</br>


<% @active_year_hash.each do |key, val| %>
<%= "#{key} had #{val} actieve cliënten"%>
</br>
<% end %>
</br>
	
		
	</tr>	
    <tr>
	  <th>Titel</th>
	  <th>Initialen</th>
	  <th>Achternaam</th>
      <th>Client sinds</th>
      <th>Voornaam</th>
      <th>Email</th>
      <th>Opmerkingen</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
		<td><%= client.title %></td>
		<td><%= client.firstname %></td> 
		<td><%= client.lastname %></td> 
		<td><%= client.date %></td>		
        <td><%= client.callname %></td>
        <td><%= client.email %></td>
        <td><%= client.warning %></td>
        <td><%= link_to 'Show', client %></td>
        <td><%= link_to 'Edit', edit_client_path(client) %></td>
        <td><%= link_to 'Destroy', client, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Client', new_client_path %>
